
function BOOT()
    -- This callback function will be called once
    screen(0) -- set screen mode to Bitmap (0)
    cls(0)    -- clear the screen
end

function LOOP()
    -- Main loop function, called in each frame
    cls(0)
end